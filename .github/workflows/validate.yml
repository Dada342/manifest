name: Validate

on:
  pull_request:
  push:
    branches: [main]

env:
  # Run JS-based actions on Node 24 (Node 20 is deprecated, forced 2026-06-16).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so the signature-date generator (prebuild) can read
          # each signatory file's add commit.
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm test
      - run: bash tests/check-component-loc.sh
      - run: npx playwright install --with-deps chromium
      - run: npm run test:checklist
